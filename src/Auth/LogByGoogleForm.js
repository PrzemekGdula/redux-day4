import React from 'react'

const LogByGoogleForm = (props) => (
    <div>
        <button
            onClick={props.onLogInByGoogleClick}
        >
            LOGIN BY GOOGLE
        </button>
    </div>
)

export default LogByGoogleForm